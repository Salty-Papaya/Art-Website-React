import React from 'react';

const AboutInfo = ({ onClose }) => {
    return (
        <div className="fixed inset-0 bg-gray-600 bg-opacity-50 flex justify-center items-center p-4 z-50">
            {"Martin Hrastic is an artist"}
        </div>
    );
};

export default AboutInfo;
